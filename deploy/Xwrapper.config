# DIVD kiosk — installed to /etc/X11/Xwrapper.config
# Allow the kiosk systemd service (not attached to a login console) to start X
# as the unprivileged 'divd' user.
allowed_users=anybody
needs_root_rights=yes
