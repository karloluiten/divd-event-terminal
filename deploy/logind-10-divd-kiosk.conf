# DIVD kiosk — installed to /etc/systemd/logind.conf.d/
# Reserve VT 3 for the /admin "open shell" getty (Ctrl+Alt+F3 from the kiosk
# lands here). Other VTs stay unallocated so visitors can't tab around.
[Login]
NAutoVTs=3
ReserveVT=3
